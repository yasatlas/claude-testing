[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "claude-testing"
version = "0.1.0"
description = "Utilities including email address validation"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = ["pytest>=7.0"]
